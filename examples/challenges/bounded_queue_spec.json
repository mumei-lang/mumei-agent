{
  "module_name": "bounded_queue",
  "description": "100% safe bounded queue operations with overflow/underflow prevention",
  "atoms": [
    {
      "name": "enqueue",
      "description": "Add an element to the bounded queue, returning the new length",
      "params": [
        {"name": "len", "type": "i64"},
        {"name": "cap", "type": "i64"}
      ],
      "requires": "len >= 0 && cap > 0 && len < cap",
      "ensures": "result == len + 1 && result <= cap && result >= 0"
    },
    {
      "name": "dequeue",
      "description": "Remove an element from the bounded queue, returning the new length",
      "params": [
        {"name": "len", "type": "i64"}
      ],
      "requires": "len > 0",
      "ensures": "result == len - 1 && result >= 0"
    },
    {
      "name": "is_full",
      "description": "Check whether the bounded queue is full (returns 1 if full, 0 otherwise)",
      "params": [
        {"name": "len", "type": "i64"},
        {"name": "cap", "type": "i64"}
      ],
      "requires": "len >= 0 && cap > 0",
      "ensures": "result >= 0 && result <= 1"
    }
  ]
}
