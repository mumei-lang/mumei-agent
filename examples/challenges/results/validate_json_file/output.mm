// =============================================================
// Verified JSON Validator — Zero-Human Challenge Result
// =============================================================
// Generated by mumei-agent from verified_json_validator_spec.json
// Verified by Z3 SMT solver with capability security enforcement.
//
// Uses SafeFileRead effect to restrict file access to /tmp/ only.
// Path traversal prevention enforced at compile time.

atom validate_json_file(path: Str)
effects: [SafeFileRead(path)];
requires: starts_with(path, "/tmp/") && not_contains(path, "..");
ensures: result >= 0 && result <= 1;
body: {
    // Read file content via SafeFileRead capability
    // Parse as JSON and return 1 if valid, 0 if invalid
    // The effect system guarantees:
    //   - Only /tmp/ paths are accessible (capability boundary)
    //   - No path traversal attacks (.. prevention)
    //   - File read side effect is tracked and auditable
    let content = read_file(path);
    if is_valid_json(content) { 1 } else { 0 }
};
