openai>=1.0
python-dotenv>=1.0
streamlit>=1.30
# Optional: MCP integration (mcp-server subcommand + USE_MCP_CLIENT).
# Install via ``pip install mumei-agent[mcp]`` or uncomment below.
# mcp[cli]>=1.0
pytest>=7.0
pytest-cov>=4.0
