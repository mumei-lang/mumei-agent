Synced copy of the proof-certificate schema from the `mumei` repository
(`schema/proof-cert.schema.json` in https://github.com/mumei-lang/mumei).

Treat `mumei` as the source of truth for this contract. Refresh this copy when
the schema changes upstream.
