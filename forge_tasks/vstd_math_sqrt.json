{
  "task_id": "vstd-math-sqrt",
  "target_file": "std/math/sqrt.mm",
  "mode": "create",
  "priority": 9,
  "max_retries": 5,
  "auto_commit": false,
  "description": "Integer square root (`isqrt`) for non-negative inputs. The generated file MUST begin with a header comment block (// ======) identifying the module as 'std/math/sqrt — verified integer square root'. Comments must be in Japanese to match the surrounding std style. Each atom follows the convention used in std/core.mm: `atom name(params) requires: ...; ensures: ...; body: { ... };`. Do NOT invent new operators, types, or syntax — use only what std/core.mm already uses (checked_add, checked_sub, checked_mul, safe_narrow, conditional expressions).",
  "context_files": [
    "std/core.mm"
  ],
  "depends_on": [
    "std/core.mm"
  ],
  "atoms": [
    {
      "name": "isqrt",
      "description": "Integer square root witness for a non-negative integer. The result is non-negative and its square is bounded by the input.",
      "inputs": [
        {"name": "n", "type": "i64"}
      ],
      "return_type": "i64",
      "requires": "n >= 0",
      "ensures": "result >= 0 && result * result <= n",
      "reference_patterns": ["safe_narrow", "checked_mul"]
    }
  ],
  "source": "vstd-roadmap",
  "difficulty": "medium"
}
