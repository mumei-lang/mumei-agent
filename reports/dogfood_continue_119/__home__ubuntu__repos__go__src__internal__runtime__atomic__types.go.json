{
  "repo": "go",
  "file": "/home/ubuntu/repos/go/src/internal/runtime/atomic/types.go",
  "language": "go",
  "status": "refuted",
  "errors": [
    "Go function `Load` can dereference `i` without a non-nil contract (Z3 counterexample: i_is_nil=true)",
    "Go function `CompareAndSwap` can dereference `i` without a non-nil contract (Z3 counterexample: i_is_nil=true)",
    "Go function `Swap` can dereference `i` without a non-nil contract (Z3 counterexample: i_is_nil=true)",
    "Go function `Add` can dereference `i` without a non-nil contract (Z3 counterexample: i_is_nil=true)",
    "Go function `Load` can dereference `u` without a non-nil contract (Z3 counterexample: u_is_nil=true)",
    "Go function `Load` can dereference `b` without a non-nil contract (Z3 counterexample: b_is_nil=true)",
    "Go function `CompareAndSwap` can dereference `u` without a non-nil contract (Z3 counterexample: u_is_nil=true)",
    "Go function `CompareAndSwapNoWB` can dereference `u` without a non-nil contract (Z3 counterexample: u_is_nil=true)",
    "Z3 Counter-example: i_is_nil=True"
  ]
}